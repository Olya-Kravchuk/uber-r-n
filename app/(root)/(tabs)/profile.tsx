import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

type Props = {};

const Prorile = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>Prorile</Text>
    </SafeAreaView>
  );
};

export default Prorile;
